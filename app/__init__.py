"""Fake application modules used by this repo's tests.

`prompts.py` and `assistant.py` stand in for the real LLM-calling code in
your own app. They exist so the Layer 1 and Layer 2 tests in this repo are
genuinely runnable on their own, without you having to wire up a real model
integration first.
"""
